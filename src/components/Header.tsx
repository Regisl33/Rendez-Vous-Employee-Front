const Header = () => {
  const title = "Create Your Personalized Services";
  const content = (
    <header className="header">
      <h1>{title}</h1>
    </header>
  );
  return content;
};

export default Header;
